import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function MentorListHeader() {
  return (
    <DialogHeader>
      <DialogTitle className="text-2xl font-bold transition-all duration-200">Explore All Mentors</DialogTitle>
    </DialogHeader>
  );
}